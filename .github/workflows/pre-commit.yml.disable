name: pre-commit

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled
  push:
    branches:
      - test_workflows_ye
    paths:
      - .github/workflows/pre-commit.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  pre-commit:
    runs-on: [ self-hosted, Linux, X64, ye_test ]
    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
      with:
        python-version: "3.12"
    - run: echo "::add-matcher::.github/workflows/matchers/actionlint.json"
    - run: echo "::add-matcher::.github/workflows/matchers/markdownlint.json"
    - run: echo "::add-matcher::.github/workflows/matchers/mypy.json"
    - uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1
      with:
        extra_args: --all-files --hook-stage manual

# 文件 / 目录权限拒绝（Permission denied） 问题
# Removing previously created refs, to avoid conflicts
# /usr/bin/git rev-parse --symbolic-full-name --verify --quiet HEAD
# refs/heads/test_workflows_ye
# /usr/bin/git checkout --detach
# Error: fatal: Unable to create '/home/yefubao/vllm_cicd/actions-runner/_work/vllm-FL/vllm-FL/.git/index.lock': Permission denied
# Warning: Unable to prepare the existing repository. The repository will be recreated instead.
# Deleting the contents of '/home/yefubao/vllm_cicd/actions-runner/_work/vllm-FL/vllm-FL'
# Error: File was unable to be removed Error: EACCES: permission denied, unlink '/home/yefubao/vllm_cicd/actions-runner/_work/vllm-FL/vllm-FL/.buildkite/check-wheel-size.py'
