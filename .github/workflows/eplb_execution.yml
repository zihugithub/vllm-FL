name: EPLB Execution Test

on:
  push:
    branches:
      - test_workflows_ye
    paths:
      - .github/workflows/eplb_execution.yml
  #     - 'vllm/distributed/eplb/**'
  #     - 'tests/distributed/test_eplb_execute.py'
  # pull_request:
  #   branches:
  #     - main
  #   paths:
  #     - 'vllm/distributed/eplb/**'
  #     - 'tests/distributed/test_eplb_execute.py'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}-${{ github.actor }}
  cancel-in-progress: true

jobs:
  ubuntu-gpu-smoke-test:
    runs-on: [ self-hosted, Linux, X64, ye_test ]
    container:
      image: localhost:5000/flagos:vllm-cuda-amd64-test-20251216
      ports:
        - 80
      volumes:
        - /home/flagscale_cicd/docker/docker_build/docker_tokenizers:/home/gitlab-runner/tokenizers
        - /home/flagscale_cicd/docker/docker_build/docker_data:/home/gitlab-runner/data
      options: --gpus all --shm-size=500g --privileged --ipc=host --ulimit memlock=-1 --ulimit stack=67108864 --ulimit nofile=65535:65535 --user root

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6.0.1
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}
          ssh-strict: true
          ssh-user: git
          persist-credentials: true
          clean: true
          sparse-checkout-cone-mode: true
          fetch-tags: false
          show-progress: true
          lfs: false
          submodules: false
          set-safe-directory: true
          retry-on-intr: fail=5,delay=180000

      - name: EPLB Execution Test
        timeout-minutes: 20
        working-directory: ${{ github.workspace }}/tests
        run: |
          pytest -v -s distributed/test_eplb_execute.py
